name: frontend_cafeicultura
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins: # Adicione isso
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Atenção aqui: 'assets' deve ter EXATAMENTE 2 espaços,
  # alinhado com o 'u' do uses-material-design.
  assets:
    - assets/images/